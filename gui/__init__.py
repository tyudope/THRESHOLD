# gui/ - tkinter presentation layer for THRESHOLD.
#
# This package is a second entry point alongside the CLI (cli/). It reuses
# all domain logic from core/, models/, and storage/ and adds nothing to the
# rules - it only draws windows. The CLI remains the canonical reference flow.
